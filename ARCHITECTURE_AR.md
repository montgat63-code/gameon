# GameMaster — قرار البنية من الصفر

## الهدف

GameMaster هو تطبيق Windows لإدارة اتصال Android وعرض الهاتف والتحكم به لأغراض مشروعة، مع واجهة Gaming احترافية وProfiles ومحرر مفاتيح وتشخيص واضح. الإصدار الأول يعتمد على **عميل Scrcpy الرسمي المطابق للخادم** ولا يعيد تنفيذ بروتوكوله الخاص داخل Python.

## مبدأ التصميم

كل طبقة لها مسؤولية واحدة. طبقة ADB تكتشف الأجهزة وتقرأ حالتها. طبقة Scrcpy تشغّل العميل الرسمي وتراقب العملية ولا تفك الفيديو يدويًا. طبقة التحكم تحفظ خرائط المفاتيح والإحداثيات وتمنع العمل قبل جاهزية الاتصال. طبقة الواجهة تعرض الحالة ولا تخفي الفشل. طبقة Profiles تحفظ الإعدادات بإصدار Schema قابل للترقية.

## المكونات

| المكوّن | المسؤولية | ما لا يفعله |
|---|---|---|
| `adb_manager.py` | تنفيذ أوامر ADB الآمنة، اكتشاف الأجهزة، وتصنيف `device/unauthorized/offline` | لا يشغّل Scrcpy ولا يرسم الفيديو |
| `official_scrcpy.py` | تشغيل `scrcpy.exe` الرسمي المطابق، تمرير الإعدادات، مراقبة stdout/stderr، والإيقاف الآمن | لا يحقن بروتوكول Scrcpy يدويًا ولا يحول H264 إلى JPEG |
| `device_state.py` | نموذج حالات موحد للجهاز والاتصال | لا ينفذ أوامر نظامية مباشرة |
| `input_mapper.py` | تحويل لوحة المفاتيح والماوس إلى أحداث GameMaster وفق Profile | لا يعمل قبل إعلان `control_ready` |
| `profile_store.py` | حفظ وقراءة والتحقق وترقية Profiles | لا يحفظ كلمات مرور أو أسرار |
| `diagnostics.py` | سجل منظم، فحص الأدوات، وتقارير قابلة للنسخ | لا يخفي الاستثناءات |
| `src/ui/` | الواجهة، اللوحات، طبقة المفاتيح، وإدارة نافذة العرض | لا تحتوي منطق ADB أو Scrcpy |

## حالة الاتصال

```text
NO_DEVICE
  -> UNAUTHORIZED
  -> DEVICE_READY
  -> SCRCPY_STARTING
  -> VIDEO_READY
  -> CONTROL_READY
  -> STREAMING
  -> RECOVERING
  -> STOPPED
```

لا يجوز تفعيل Keymapper في أي حالة قبل `CONTROL_READY`. عند فشل Scrcpy أو اختفاء الجهاز، تتوقف الأحداث، تُغلق العملية، ويظهر سبب واحد قابل للنسخ.

## قرار العرض

يُستخدم عميل Scrcpy الرسمي مع عرضه الأصلي لتجنب مسار H264 إلى JPEG إلى Pillow إلى Qt. GameMaster يدير النافذة والإعدادات واللوحات، لكنه لا يعيد ترميز الفيديو ولا يملك socket خاصًا ببروتوكول Scrcpy.

## الأمن والخصوصية

لا تُشغّل أوامر Shell مع نص غير موثوق. تُمرر الأوامر كقائمة معاملات عند الإمكان. لا تُرفع مفاتيح أو بيانات الهاتف إلى مستودع عام. أي صلاحيات Android مثل USB Debugging أو Accessibility يفعّلها المستخدم بنفسه ويُشرح سببها.

## تعريف الإنجاز

لا تعتبر الميزة مكتملة إلا إذا نجحت اختبارات الوحدة، واختبار الواجهة، وفحص البناء، ثم اختبار الهاتف الحقيقي المرتبط عبر USB. لا يكفي أن يفتح البرنامج أو يظهر أول إطار.

## أدوار المنصات

| المنصة | متى نستخدمها؟ | الناتج |
|---|---|---|
| GitHub | من البداية كمصدر واحد للكود | مستودع، Issues، وتاريخ تغييرات |
| Codespaces | عند كتابة الكود من الهاتف أو اللابتوب الضعيف | بيئة VS Code سحابية |
| Jules | لتحليل المستودع أو تنفيذ مهمة كبيرة محددة | Commit/Pull Request للمراجعة |
| Amazon Q | لمهام إصلاح وتشغيل محلية داخل البيئة | تعديل واختبارات وسجل أوامر |
| GitHub Actions | بعد كل Commit أو عند Tag | اختبارات وZIP/EXE |
| اللابتوب والهاتف | في مرحلة التحقق النهائي فقط | اختبار USB وADB وScrcpy الحقيقي |
