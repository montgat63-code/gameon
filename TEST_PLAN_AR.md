# خطة اختبار GameMaster

## بوابة الصياغة

يجب أن ينجح `python -m compileall app src tests` دون تحذيرات قاتلة. يجب أن ينجح `python -m pytest -q` قبل إنشاء أي Artifact.

## اختبارات النواة

يُختبر تحليل مخرجات ADB في حالات `device` و`unauthorized` و`offline`. تُختبر قراءة Profiles وحفظها وترقيتها، وتُختبر الإعدادات بالحفظ الذري. تُختبر خرائط المفاتيح بحيث لا تنتج حدثًا عندما يكون التحكم غير جاهز.

## اختبارات الواجهة

تُنشأ النافذة في وضع `QT_QPA_PLATFORM=offscreen`. تُفحص حالة عدم وجود جهاز، ورسالة فشل الأدوات، وتغيير حجم النافذة، وإخفاء اللوحة اليسرى واليمنى، وتكبير العرض وإعادته إلى 100%.

## اختبارات العميل الرسمي

تُفحص قائمة معاملات Scrcpy دون تشغيل الهاتف. يجب أن تحتوي على العميل الرسمي وserial والدقة وFPS وBitrate، وألا تحتوي على `stay_awake=true` أو أي socket يدوي. يجب أن تتوقف العملية بأمان عند الإغلاق.

## اختبار Windows محلي

يُفك Artifact في مجلد جديد، ويُشغل `run.bat`، ويُفحص وجود Python وADB وScrcpy والـDLLs. يجب أن تكون رسالة الملف المفقود واضحة وألا يفتح البرنامج بمسار ناقص.

## اختبار الهاتف الحقيقي

بعد تفعيل USB Debugging وقبول RSA، يجب أن يظهر الهاتف بحالة `device`. يُختبر بدء البث، تغير الإطار مع حركة الهاتف، ضغط زر واحد، سحب، إعادة توصيل الكابل، إغلاق البرنامج، وإعادة التشغيل. يجب حفظ سجل الفشل عند كل محاولة.

## معيار Release

لا تُنشر النسخة إلا بعد نجاح كل اختبارات النواة والبناء، ونجاح اختبار هاتف حقيقي موثق بصورة أو سجل. إذا فشل التحكم مع استمرار الفيديو، تُسجل الحالتان منفصلتين ولا تُخفى المشكلة برسالة عامة.
